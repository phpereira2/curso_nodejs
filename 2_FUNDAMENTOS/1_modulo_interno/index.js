const meuModulo = require('./meu_modulo')
const soma = meuModulo.soma // Simplificação de sintaxe ao longo do código (Ex: linhas 4 e 5)

soma(2, 3)
soma(5, 10)
meuModulo.soma(10, 20) // Forma completa de utilizar o módulo interno criado